language: node_js
stages:
  - linting
  - name: documentation
    if: branch = DEV
  - tests
  - name: deploy
    if: branch = master

jobs:
  include:
    - stage: linting
      script: 'echo "running linting"'
    - stage: documentation
      script: 'echo "running documentation"'
    - stage: tests
      script: 'echo "running documentation"'
    - stage: deploy
      script: 'echo "running documentation"'
